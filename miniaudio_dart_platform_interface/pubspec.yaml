name: miniaudio_dart_platform_interface
description: A common platform interface for the MiniaudioDart package.
version: 1.0.4
publish_to: none
repository: https://github.com/MichealReed/miniaudio_dart/tree/master/miniaudio_dart_platform_interface
#####

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  miniaudio_dart_ffi:
    path: ../miniaudio_dart_ffi
  miniaudio_dart_web:
    path: ../miniaudio_dart_web

dev_dependencies:
  flutter_lints: ^2.0.1
